services:
  infoscreen:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - PUID=1000
      - PGID=1000
    restart: unless-stopped
